图像语义分割：属计算机视觉中图像理解（像素级别）的一环。语义分割：Semantic Sementation 
历史：无监督图像区域分割方法，是指将图像分成若干互不重叠的区域，各区域内部有某一种一致的属性或特征，但相邻区域之间的特征和属性具有差异性。
如前景-背景分割方法、图片内容的聚类（比如使用mean-shift算法基于像素的颜色）。
前DL（Deep Learning）时代的语义分割方法可谓“百花齐放”。
1，基于机器学习的全监督图像语义分割方法需要足够的精确到像素级标注的训练图像集，然而，利用LabelMe等标注工具，人工完成一副图像的标注时间10-20min，需要极大
的人工成本。
2，弱监督语义分割算法（具有一定的训练数据优势）。例如用外接矩形框(bounding box)标识图像区域，只给出图像层面的语义标签，甚至给出不完备的带噪声的语义标签
。当今互联网上存在着海量图像数据，许多图像都带有用户标注的富有噪声的文本描述信息，然而有像素级别的精确标注信息的图像数据却非常稀少。
